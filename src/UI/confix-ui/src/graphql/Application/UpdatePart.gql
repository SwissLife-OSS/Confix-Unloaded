mutation updatePart($input: UpdateApplicationPartInput!) {
  updateApplicationPart(input: $input) {
    application {
      id
      name
      parts {
        id
        name
        components {
          id
          name
        }
      }  
    }
  }
}
