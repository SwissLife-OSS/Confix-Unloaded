query GetComponents {
  components {
    nodes {
      ...Component
    }
  }
}

query GetComponentById($id: ID!){
  componentById(id: $id){
    ...Component
  }
}

# components(
#    applicationId: ID
#    applicationPartId: ID
#    namespace: String
#    search: String
#    first: Int
#    after: String): ComponentsConnection
# componentById(id: ID!): Component
